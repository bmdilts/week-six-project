<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="/../public/main.css">
    <title>Gabble</title>
  </head>
  <body>
    <header>
      <a href="./home.mustache">Gabble</a>
      <a href="./new.mustache">Create a new gab</a>
      <a href="./login.mustache">Logged in as{{displayName}}</a>
      <a href="">Log out</a>
    </header>
    <main>
      <div class="gabs">
        {{#gabs}}
        <div>
          <p>{{gab}}</p>
          <p>{{date}}</p>
          <p>{{likes}}</p>
        </div>
        {{/gabs}}
        <form action="/like" method="post">
          <button class="button" type="submit" value="like"></button>
          <button class="button" type="submit" value="unlike"></button>
        </form>
      </div>
    </main>
  </body>
</html>
